makeContent({
  "content" : "<div id='effect-resize' class='content-wrapper'><h1>缩放</h1><h2>概述</h2><p>为一个元素实现缩放效果，可以朝右、下、右下方进行拖动缩放<br>若此元素是table，需要在table外加一个包裹元素</p><h2>示例</h2><div class='exam-area'><div class='resize-node'></div></div><div class='code-wrapper'><h2>代码</h2><pre><code>var nodeToResize = document.querySelector('.resize-node')<br>naiveUI.resizeable(nodeToResize)</code></pre></div><h2>API</h2><div class='api'></div><h2>备注</h2><p>如果需要为一个元素同时设置拖曳和缩放效果，由于当元素为table时需要外套一层包裹元素，因此需要先设置缩放，拿到返回的元素，再拿这个元素去设置拖曳<br>这也是缩放不返回操作接口的原因</p><code>var a = naiveUI.resizeable(nodeToResize),<br>&nbsp&nbspb = naiveUI.dragable(a,a,dragArea)<br><br>b.setDrag()</code></div>"
})